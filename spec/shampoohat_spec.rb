require 'spec_helper'

describe Shampoohat do
  it 'has a version number' do
    expect(Shampoohat::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
